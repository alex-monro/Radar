import React from "react";

type Props = {};

const Issues = (props: Props) => {
  return (
    <section className="flex flex-col gap-4">
      <h2 className="text-3xl font-semibold">
        Issues <span className="text-3xl text-muted">(8)</span>
      </h2>
    </section>
  );
};

export default Issues;
